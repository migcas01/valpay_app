// This page is no longer used in routing.
// The lookup flow now goes directly through PayPage → PaymentWizardPage.
// Kept to avoid broken imports during transition.
export function PayLookupPage() {
  return null;
}
